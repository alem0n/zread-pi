# v1.13.5

## 文档修正：组件级测试计数同步为实际值

`test:components` 的实际用例数为 **51**（3 个文件），但文档里多处仍停留在
旧计数（49 / 39），纯文档漂移，不涉及任何代码或行为变化。

### 改动

- `AGENTS.md` §2：`test:browse` 行的「+ 组件 49」、`test:components` 行的
  「49/49」、`test:tui` 行汇总里的「+ 49（组件）」、§6.7 里的
  「`test:components`，39 项」→ 统一为 51
- `MIGRATION.md` §26.11：首批用例「49 项」→「51 项」、验证段「49/49」→「51/51」

### 验证

- `bun run test:components`：51 pass / 0 fail（3 个文件）
- `bun run typecheck`：0 错误
