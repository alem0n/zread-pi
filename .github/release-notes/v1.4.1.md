# v1.4.1 — Backfilled release notes for historical versions

Documentation-only release: no code behavior changes.

## 📝 What changed

- Backfilled complete release-note bodies for the six historical versions
  `v0.14.0` / `v0.15.0` / `v1.0.0` / `v1.1.0` / `v1.2.0` / `v1.3.0`
  (`.github/release-notes/`, +361 lines) — previously short placeholders.
  CI uses these files as the Release body, so the GitHub Releases for those
  tags now display the full changelog content.

## ✅ Verification

- `bun run typecheck` — 0 errors (docs-only change).
