# DESIGN.md — Notion 风格设计系统（终端适配版）

本文件是 zread-pi TUI 与浏览站的视觉规范来源。`apps/cli/src/theme.ts` 的色值与本文档一致；
修改任何 UI 前先读这里。原设计系统为 Web 规范，本文在每节末尾给出**终端适配约定**。

---

## 1. 视觉主题与氛围

Notion 的设计哲学：一块不碍事的空白画布。系统建立在**暖中性色**而非冷灰之上，
呈现一种「优质纸张」而非「无菌玻璃」的亲和极简主义。

- 暖灰色阶（`#f6f5f4` / `#31302e` / `#615d59` / `#a39e98`）带黄棕底调，有触感温度；
- 文本不是纯黑，而是暖近黑（Web 为 `rgba(0,0,0,0.95)`）；
- 边框哲学是「耳语」：`1px solid rgba(0,0,0,0.1)`，只做几乎不可察觉的分隔，不用重边框；
- 阴影多层叠加，单层不透明度不超过 0.05，深度「被感觉到而非被看到」；
- Notion Blue（`#0075de`）是核心 UI 中唯一的饱和色，只用于 CTA 与交互元素。

**终端适配约定：** 终端没有半透明与像素阴影。暖近黑直接用 `#31302e`（warm dark）表达层次，
「耳语边框」降级为 ASCII 圆角框（`╭─╮ │ ╰─╯`，见 `apps/cli/src/tui/components/rounded-box.ts`），
层次靠背景/前景色深浅与留白表达，不引入额外装饰字符。

## 2. 色彩与角色

### 主色

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| Notion Blue | `#0075de` | 主 CTA、链接、交互强调——核心 UI 唯一饱和色 |
| 纯白 | `#ffffff` | 页面背景、卡片表面（Web） |
| 暖近黑 | `rgba(0,0,0,0.95)`（终端用 `#31302e`） | 主文本、标题 |

### 暖中性色阶（本项目的基色调）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 暖白 | `#f6f5f4` | 背景色调、分区交替、浅色卡片填充 |
| 暖暗 | `#31302e` | 深色表面、深色分区文本 |
| 暖灰 500 | `#615d59` | 次要文本、描述、弱化标签 |
| 暖灰 300 | `#a39e98` | 占位文本、禁用态、说明文字 |

### 语义色

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| Teal | `#2a9d99` | 成功状态、正向指示（TUI 的 success） |
| Green | `#1aae39` | 确认、完成徽标 |
| Orange | `#dd5b00` | 警告、加载中、注意提示（TUI 的 warning） |
| 错误红 | `#e54847` | 错误状态（终端标准红，theme.ts） |

### 状态徽标色（生成 / 同步页面）

- `new` → Orange `#dd5b00`；`updated` → Notion Blue `#0075de`；
- `unchanged` → Teal `#2a9d99`；`archived` → 暖灰 300 `#a39e98`。

**终端适配约定：** 上述色值经 ANSI 24-bit 真彩色下发（`apps/cli/src/tui/ansi.ts`）；
不支持真彩色的终端由终端自身降级映射。**不要**在页面代码里硬编码色值，一律从 `theme.ts` 取。

## 3. 字体规则

Web 端以 NotionInter（改版 Inter）为骨干；**终端适配约定**：TUI 不加载字体，只约束字号权重不可控的
替代维度——**层级靠留白 + 大小写 + 符号前缀**：

- 大标题：独立成行 + 上下各一空行，不加粗装饰（pi-tui 无粗体依赖时靠位置表达）；
- 小节标题：`§` 或加粗（ANSI bold）+ 后接空行；
- 正文：常规密度（每行不超终端宽度，`visibleWidth` 截断）；
- 徽标 / 状态：`[NEW]`、`(n/总数)` 这类括号包裹形式，宽度计入布局；
- 数字对齐：列表计数、token 用量等右对齐展示。

Web 端（`apps/browse`）沿用原规范：Display 64px / 字重 700 / letter-spacing −2.125px，
正文 16px / 400 / 1.50，徽标 12px / 600 / +0.125px（唯一正字距）。

## 4. 组件样式

### 按钮（Web）

- **主按钮**：`#0075de` 底 + 白字，padding 8px 16px，radius 4px，hover 变深 `#005bab`；
- **次按钮**：`rgba(0,0,0,0.05)` 底 + 近黑字；
- **幽灵 / 链接**：透明底，hover 下划线；
- **药丸徽标**：`#f2f9ff` 底 + `#097fe8` 字，radius 9999px，12px/600。

### 列表与选择（TUI 核心组件）

- 选中项：前景切换为 Notion Blue，前缀 `❯` 或反色条（与迁移前 Ink 版观感一致）；
- 列表**窗口化渲染**：只画可见窗口，末尾追加 `↑ (12/63) ↓` 位置指示；
- 目录条目补 `/` 后缀；禁用项用暖灰 300；
- 分隔线用 `Divider`（单行 `─` 重复，颜色暖灰 300）。

### 输入框

- 占位符暖灰 300 `#a39e98`，正文暖近黑；
- 光标用反色假光标（与迁移前一致）；
- 校验错误行用错误红，紧跟在输入框下一行，不弹窗。

### 卡片 / 信息框（Web）

- 白底 + 耳语边框 + radius 12px（重点卡 16px）；
- 多层阴影（单层 opacity ≤ 0.05）。

**终端适配约定：** `RoundedBox` 撑满终端宽度、paddingX=1，边框即「卡片」；
不做 hover 阴影，强调靠边框色 / 反色。

## 5. 布局原则

- 基准间距 8px；Web 缩放：2/4/8/12/16/24/32/48/64px；
- 大区间距宽裕（Web 64–120px），终端版面紧凑但**每页至少一段留白**分隔标题与正文；
- 白色分区与暖白分区交替（Web）；终端用空行 + Divider 交替；
- 内容最大宽度约 1200px（Web）；终端版面跟随终端宽度，超宽行一律截断（pi-tui 渲染器会抛错）。

**终端适配约定（硬性）：** 任何页面的渲染行数不得超过终端高度——列表必须窗口化
（`computeItemWindow` / `scrollIndicator`），非列表内容过长时分步展示，不允许「整页铺开」。

## 6. 层次与深度

| 层级 | Web 处理 | 终端处理 |
| --- | --- | --- |
| 平面 | 无阴影无边框 | 纯文本 |
| 耳语 | `1px solid rgba(0,0,0,0.1)` | RoundedBox / Divider |
| 软卡 | 4 层阴影（≤0.04） | 边框色加深一级 |
| 深卡 | 5 层阴影（≤0.05，52px blur） | 反色块（选中态 / 弹出列表） |
| 焦点 | 2px 蓝色 outline | 光标行反色 + Notion Blue 前缀 |

## 7. 响应式与状态

- Web 断点：400 / 600 / 768 / 1080 / 1200 / 1440px；
- 终端：窗口缩放时**立即重算**列表窗口行数（终端行数 − 布局头部 − 文案），`test:tui` 覆盖自适应断言；
- 交互状态：默认 → hover（色移）→ active（按压变深）→ focus（蓝圈）→ disabled（暖灰 300）；
  终端对应：普通 → 聚焦（蓝/反色）→ 加载中（80ms 帧动画 `⠋⠙⠹…`）→ 禁用（暗色）。

## 8. Agent 提示速查（写 UI 时照抄）

- 主 CTA / 链接 / 选中态：`#0075de`
- 页面底色：白（Web）/ 默认终端色（TUI）
- 次级背景：`#f6f5f4`
- 标题与正文：暖近黑（终端 `#31302e`）
- 次要文本：`#615d59`；弱化 / 占位：`#a39e98`
- 成功 `#2a9d99` · 警告 `#dd5b00` · 错误 `#e54847`
- 边框永远是最轻的那一档；装饰字符克制，不用 emoji

### 迭代守则

1. 一律暖中性色，禁止蓝灰；
2. 核心 UI 只允许 Notion Blue 一个饱和色，语义色仅用于状态；
3. 边框只降不升——拿不准就用最轻的；
4. 层次优先用留白与色深，其次才是边框；
5. 所有色值从 `apps/cli/src/theme.ts` 引用，不允许页面内散落十六进制字面量。
